name: Hello Agile

on:
  push:
    branches: [ dev ] #CI Workflow Example

  pull_request:
    branches: [ main ]

jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions!"